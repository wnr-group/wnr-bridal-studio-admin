# Supabase project (get from: Dashboard → Project Settings → API)
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Service role key — server-side only, NEVER expose to browser
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Razorpay — all keys server-side only, NEVER use NEXT_PUBLIC prefix
RAZORPAY_KEY_ID=rzp_test_your_key_id_here
RAZORPAY_KEY_SECRET=your_razorpay_key_secret_here
RAZORPAY_WEBHOOK_SECRET=your_webhook_secret_from_razorpay_dashboard

# Shared secret for verifying requests from the wnr-bridal-studio storefront
# Must match STOREFRONT_API_SECRET in wnr-bridal-studio — generate with: openssl rand -hex 32
STOREFRONT_API_SECRET=replace_with_secure_random_string
